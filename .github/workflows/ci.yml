name: CI

on:
  push:
    branches: [main]
  pull_request: {}

jobs:
  test:
    # Pin the image rather than best-effort-selecting an Xcode version: the test
    # target uses swift-testing (`import Testing`), which a Swift 5.x toolchain
    # cannot compile at all. macos-26 matches the runner the release job uses.
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Show Swift toolchain version
        run: swift --version

      - name: Build
        run: swift build

      - name: Test
        # RUN_KEYCHAIN_TESTS is intentionally left unset: the keychain
        # round-trip test requires an interactive/unlocked keychain and
        # must stay skipped in CI.
        run: swift test

  app-build:
    # Builds the macOS .app target (XcodeGen + xcodebuild) unsigned, so a
    # broken app target fails CI even though it isn't exercised by the SPM
    # test suite above. Same image as the release job, so CI compiles against
    # the same (macOS 26) SDK that ships.
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate project
        run: xcodegen generate

      - name: Build app (unsigned)
        run: |
          xcodebuild -project ClaudeStatusBar.xcodeproj -scheme ClaudeStatusBar \
            -configuration Debug CODE_SIGNING_ALLOWED=NO build
